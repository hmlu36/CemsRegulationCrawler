# Optional generic webhook (Slack/Teams-compatible endpoint or relay)
CEMS_WEBHOOK_URL=
CEMS_REQUEST_DELAY=0.8

# Optional email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO=
